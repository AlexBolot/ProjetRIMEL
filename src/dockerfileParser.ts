import * as DockerfileParser from 'dockerfile-ast';
import { AstExplorer} from "./astExplorer";


export function parsingDockerfile(files){
  console.log(files);
  let content="";
  for(var file in files){
    //let AstExplorer = new AstExplorer(file);
  }

}